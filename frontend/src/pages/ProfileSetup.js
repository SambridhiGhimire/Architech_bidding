import React from "react";
import { Navigate } from "react-router-dom";

const ProfileSetup = () => {
  // This page will be implemented later for additional profile setup
  return <Navigate to="/dashboard" />;
};

export default ProfileSetup;
